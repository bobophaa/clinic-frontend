# Use /api in dev — Vite proxies to Laravel (see vite.config.js). No CORS issues.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
# Direct URL (only if not using Vite dev server):
# VITE_API_BASE_URL=http://127.0.0.1:8000/api
